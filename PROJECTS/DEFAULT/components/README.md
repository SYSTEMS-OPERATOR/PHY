# DEFAULT Components

Project-local component guides for DEFAULT prototype parts.

Use `PROJECTS/component_guide.template.json` as the record contract.

DEFAULT component guides are allowed to be draft-oriented, but must still preserve explicit dimensions, unknowns, validation status, and references.